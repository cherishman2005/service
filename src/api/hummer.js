//import { Hummer, ChatRoom } from 'Hummer';


/*
const platform = new Hummer.Platform({appid: 100, uid: 10});

const chatroom = new Hummer.ChatRoom(platform, {
	onRecvSingleUserData
});

function onRecvSingleUserData(data) {
	console.log("onRecvSingleUserData: " + data);
}

chatroom.testChatRoom();
*/

const chatroom = new Test({/*token_type: "YY_TOKEN"*/});

export default chatroom;
